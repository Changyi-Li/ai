# SQL Anywhere MCP Server Configuration
#
# Copy this file to .env and fill in your database connection details

# Option 1: Use a complete ODBC connection string (recommended)
# SQL Anywhere uses DBN (DataBase Name) parameter, not DATABASE
# For shared memory connections (default, fastest for local):
SQLANYWHERE_CONNECTION_STRING="DRIVER={SQL Anywhere 17};ServerName=myserver;DBN=001;UID=dba;PWD=sql"

# For TCP/IP connections:
# SQLANYWHERE_CONNECTION_STRING="DRIVER={SQL Anywhere 17};ServerName=myserver;Host=localhost:2638;DBN=001;UID=dba;PWD=sql"

# Option 2: Specify individual connection parameters
# SQLANYWHERE_DATABASE=001       # Maps to DBN parameter (DataBase Name)
# SQLANYWHERE_USER=dba           # Maps to UID parameter
# SQLANYWHERE_PASSWORD=sql       # Maps to PWD parameter
# SQLANYWHERE_DRIVER="SQL Anywhere 17"
# SQLANYWHERE_SERVER_NAME=myserver  # Server name (required)
# SQLANYWHERE_HOST=localhost     # For TCP/IP connections
# SQLANYWHERE_PORT=2638          # For TCP/IP connections
# SQLANYWHERE_USE_TCP=false      # Set to 'true' to force TCP/IP instead of shared memory

# Security Settings
# SQLANYWHERE_AUTHORIZED_USERS=monitor,ExtensionsUser  # Only expose schema from these users

# Query Settings
# SQLANYWHERE_QUERY_TIMEOUT=30  # Query timeout in seconds
# SQLANYWHERE_MAX_ROWS=1000      # Default row limit for queries
# SQLANYWHERE_MAX_ROWS_LIMIT=10000  # Maximum allowed row limit
